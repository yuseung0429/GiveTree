import { style } from '@vanilla-extract/css';

export const container = style({
  position: 'absolute',
  left: '1rem',
  bottom: '1rem',
});

export const rounded = style({
  width: '2.75rem',
  padding: '0 !important',
});

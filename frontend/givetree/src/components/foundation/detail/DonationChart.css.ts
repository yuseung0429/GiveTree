import { style } from '@vanilla-extract/css';

export const doughnutChart = style({
  position: 'absolute',
  top: '50%',
  left: '50%',
  translate: '-50% -50%',
});

export const doughnutText = style({
  position: 'absolute',
  top: '50%',
  left: '50%',
  translate: '-50% -50%',
});
